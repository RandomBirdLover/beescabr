# Functions

Every function called from a file other than its own -- the project's API.
A function used only inside one script is not here; read it in place.

**195 functions across 74 files.** Grouped by the folder that defines them.

*Generated by `scripts/utils/function_docs.R`. Do not edit by hand --
edit the `#'` block above the function and regenerate.*


## `scripts/analysis/richness/`

### rarefaction_vegan.R

| Function | What it does |
|---|---|
| `draw(M, key, title, rank, cols, group_fill)` | Draw and write one rarefaction figure |


## `scripts/analysis/shared/`

### conservation_status.R

| Function | What it does |
|---|---|
| `conservation_label(sp)` | IUCN flag label, e.g. "IUCN Endangered"  ("" = not at-risk) |
| `flagged_species(codes)` | species in the given IUCN categories (default: at-risk CR/EN/VU/NT), as a data.frame |
| `iucn_cache_exists()` | TRUE when the IUCN status cache has been fetched |
| `iucn_code_of(sp)` | IUCN red-list code for each species |
| `iucn_name_of(sp)` | IUCN red-list category, spelled out for a reader |

### explorer_photo_helpers.R

| Function | What it does |
|---|---|
| `fetch_taxon_photo(name, rank, open_lic, request_fn, sleep_fn)` | Search for the taxon; if its default photo is closed, fetch the detail record |

### folder_readmes.R

| Function | What it does |
|---|---|
| `write_folder_readmes(root)` | Write a WHAT_THESE_FILES_ARE.txt into every folder that has a note |

### forage_selectivity.R

| Function | What it does |
|---|---|
| `forage_preference_label(genus, plant_fmt, min_rec)` | One-line forage-preference verdict for a genus, for the field guide. |
| `forage_preference_label_species(species, plant_fmt, min_rec)` | One-line forage-preference verdict for a SPECIES (Genus epithet), for the species field guide. |
| `selective_genera(min_rec)` | Genera that use plants more selectively than availability predicts |
| `selectivity_table(min_rec)` | SEASON + YEAR + METHOD-AWARE selectivity. For each bee genus we ask: given the exact |
| `selectivity_table_species(min_rec)` | Per-species version of the forage-selectivity table |

### inat_taxon_links.R

| Function | What it does |
|---|---|
| `bee_taxon_id(ids, ranks)` | Every cleaned record already carries a taxon_id. Scripts used to drop it during |
| `inat_photo_link(taxon_id, scientific_name)` | iNaturalist photo link, as an HTML anchor carrying the iNat logo |

### inext_estimates.R

| Function | What it does |
|---|---|
| `inext_estimates_tidy(asy, est_size, est_cov, rank)` | asy: iNEXT()$AsyEst. est_size / est_cov: estimateD(base = "size" / "coverage"). |

### not_on_holway.R

| Function | What it does |
|---|---|
| `format_new_bees(noh, mode)` | format_new_bees(): the grouped, numbered "N new bees not in Holway's Checklist" text block. |
| `holway_id_set(holway_path)` | holway_id_set(): taxon_ids present in Holway's reference (as character). A CABR checklist taxon whose |
| `holway_name_set(holway_path)` | holway_name_set(): lower-case binomials Holway lists, from BOTH its scientific_name and its complex |
| `not_on_holway_bees(checklist_path, spec, inat, holway_path)` | not_on_holway_bees(): one row per CABR-checklist taxon with holway == FALSE, annotated with |

### plant_names.R

| Function | What it does |
|---|---|
| `plant_common_name(genus)` | Vectorised over a character vector of scientific genera. |
| `plant_label(genus, sci_wrap)` | `sci_wrap` optionally wraps the Latin part (e.g. "<i>%s</i>" for HTML italics). |
| `plant_label_expr(genus)` | Returns an expression vector for text()/axis(): "Common Name (italic Genus)". |

### rarefaction_names.R

| Function | What it does |
|---|---|
| `rare_out_name(dim, rank, kind)` | kind: "figure" / "table" -> one per rank, sharing a stem so they sort together. |
| `rare_takeaway(veg, dim)` | The subtitle sentence for a rarefaction figure |
| `rare_window(dim)` | NULL for a report dimension (by_transect / by_year), which has no fair window |
| `rare_window_dir(dim)` | Output subfolder for a comparison's fair window |
| `rare_window_records(rec, dim)` | `methods = NULL` means keep both; naming a method is how the observer window |

### theme_beescabr.R

| Function | What it does |
|---|---|
| `bee_badge_css(bg, fg, sel)` | turn a background + foreground pair into CSS rules; `sel` maps each key to its full selector. |
| `bee_base_par()` | House `par()` defaults for a base-R figure |
| `bee_caption_base(cex, line0)` | base-R figures draw the caption in the BOTTOM OUTER margin (ggplot uses labs(caption=)). |
| `bee_data_asof(path)` | The date the data was last pulled, for a figure caption |
| `bee_ggsave(filename, plot, dpi)` | 400 dpi (was 300) -- inches-based, so just more pixels, same layout |
| `bee_low_n(n)` | TRUE for counts too small to claim a preference from |
| `bee_low_n_mark(n)` | asterisk (universal glyph) on sparse taxa |
| `bee_png(filename)` | Open a PNG device at house resolution |
| `bee_sort_mark_js()` | Marker JS shared by every sortable table (both field guides + least-sampled bees), so |
| `bee_table_css()` | The shared stylesheet (the rules that go BETWEEN <style> and </style>). A script appends its own |
| `bee_test(name, stats)` | Significance clause that always NAMES the test, so a caption says WHICH test ran, not just the numbers. |
| `beescabr_fill_colors(x)` | Substitute theme colors into a CSS or JS template |
| `scope_cap(scope, method, rank, n, sig, source, asof, width, control)` | Build the caption string. scope_cap() keeps its old 3-arg shape so existing calls still work, |
| `theme_beescabr(base_size)` | The house ggplot theme |

### transect_years.R

| Function | What it does |
|---|---|
| `read_transect_years(path)` | Read the hand-maintained table of which years each transect was walked |
| `transects_in_year(year, years)` | transects_in_year(): PURE. Codes active in `year`, sorted. NULL year = all of them |


## `scripts/checklists/`

### cabr_bee_checklist.R

| Function | What it does |
|---|---|
| `build_cabr_bee_checklists(bees_sf, lookup, specimens, holway_sub)` | bees_sf   : RAW iNat bee observations as an sf POINT layer in PROJECT_CRS (taxon_id carried). |

### checklist_build.R

| Function | What it does |
|---|---|
| `build_checklist(obs_df, label, verbose)` | Reduce observations to one row per taxon |
| `combine_checklists(sources)` | Merge per-source checklists into one backbone with a flag per source |
| `finalize_checklist(checklist, taxonomy_lookup)` | Attach the taxonomy lookup and reduce names to bare epithets |
| `lookup_subtree(lookup, present_df, label, verbose)` | lookup_subtree(): the normalized subtree (leaves + ALL ancestor rows) of present_df's taxa, |
| `run_qc(checklist, label)` | Print a per-tier quality-control summary |
| `spatial_split(points_sf, boundary, label, verbose)` | Keep only the observations that fall inside a boundary |
| `taxonomy_lookup_from_bees(bees)` | The subgenus and complex columns the bee tables already carry, keyed by id |

### pl_bee_checklist.R

| Function | What it does |
|---|---|
| `build_pl_bee_checklists(bees_sf, lookup)` | Build the Point Loma tier of checklists and write them out |

### sd_bee_checklist.R

| Function | What it does |
|---|---|
| `build_sd_bee_checklists(bees_sf, lookup, holway_sub, holway_reference)` | holway_sub: the Holway subtree (lookup_subtree of the Holway reference), built once by the caller |


## `scripts/`

### config.R

| Function | What it does |
|---|---|
| `beescabr_require(pkgs, have_fn, stop_fn)` | beescabr_require(): the per-script dependency guard. CHECKS, never installs. |
| `check_paths(paths, kinds, stage)` | Report every PATHS entry whose file is missing when the contract says it should be |


## `scripts/inat_observations/`

### build_field_id_map.R

| Function | What it does |
|---|---|
| `build_field_id_map(out, con)` | Map iNaturalist observation-field names to their numeric ids |


## `scripts/inat_observations/clean/`

### bee_forage.R

| Function | What it does |
|---|---|
| `write_bee_forage(export_path, crosswalk_path, membership_path, out_path, verbose)` | write the forage list to cabr_inat_bee_forage_generated.csv (the lookup's in-park source). |

### inat_bee_clean.R

| Function | What it does |
|---|---|
| `inat_bee_clean(membership_path, export_path, crosswalk_path, transect_path, road_path, out_clean, write)` | Clean the raw iNaturalist bee observations into the analysis table |

### inat_plant_clean.R

| Function | What it does |
|---|---|
| `inat_plant_clean(membership_path, export_path, crosswalk_path, roster_path, transect_path, road_path, out_clean, write)` | Clean the raw iNaturalist plant observations into the analysis table |


## `scripts/inat_observations/engine/api/`

### inat_auth.R

| Function | What it does |
|---|---|
| `inat_auth_enabled()` | auth is ON only when an app client id is present (so an unfilled file = unauthenticated) |
| `inat_auth_login()` | inat_auth_login(): the signed-in iNaturalist username, or "" when unauthenticated. |
| `inat_auth_prompt(say)` | inat_auth_prompt(): ask for credentials ONCE at the start of a run if none are present. |
| `inat_auth_token(force)` | The iNaturalist API token for this session, fetched once and reused |

### inat_cache.R

| Function | What it does |
|---|---|
| `get_taxa_by_name(con, name, request_fn, verbose)` | Taxa matching a name, from the cache or the API |
| `get_taxon_by_id(con, id, request_fn, verbose)` | One taxon by its id, from the cache or the API |
| `prefetch_taxa(con, taxon_ids, request_fn, throttle, sleep_fn, verbose)` | Warm the cache for many taxa in a few throttled requests |
| `resolve_taxonomy(con, taxon_ids, request_fn, throttle, verbose)` | Full rank columns for a set of taxon ids |

### inat_flatten.R

| Function | What it does |
|---|---|
| `flatten_observation(o)` | Flatten one API observation into a single-row data frame |
| `parse_taxon_ranks(taxon)` | Spread a taxon's ancestry across the standard rank columns |

### inat_http.R

| Function | What it does |
|---|---|
| `inat_fetch_taxa_by_ids(ids, request_fn)` | Fetch MANY taxa in one request. iNat's /taxa/{ids} accepts a comma-separated |
| `inat_fetch_taxa_by_name(name, request_fn)` | Search the iNaturalist taxa endpoint by name (no caching) |
| `inat_fetch_taxon_by_id(id, request_fn)` | Fetch one taxon by id from the iNaturalist API (no caching) |


## `scripts/inat_observations/engine/db/`

### compact_store.R

| Function | What it does |
|---|---|
| `db_compact_if_needed(path, threshold, say)` | db_compact_if_needed(): the step that runs after an ingest. Asks the file how |

### decision_store.R

| Function | What it does |
|---|---|
| `decision_get(con, search_term)` | Return the recorded decision for a search term, or NULL if undecided. |
| `decision_put(con, search_term, action, chosen_taxon_id)` | Record what a human decided about an ambiguous name |

### observations_store.R

| Function | What it does |
|---|---|
| `clear_observations(con)` | Empty the observations table |
| `count_observations(con)` | How many observations the cache holds |
| `max_observation_id(con)` | Highest stored observation id -- the incremental fetch cursor. 0 when empty. |
| `read_observations_raw(con)` | Return raw observation JSON strings (id + raw_data) for the flatten layer. |
| `write_observations_page(con, raw_text)` | Write one page of API results into the cache |

### store_conn.R

| Function | What it does |
|---|---|
| `store_connect(path, read_only)` | Open the DuckDB observation cache |
| `store_disconnect(con)` | Close a cache connection and shut the database down |

### taxon_store.R

| Function | What it does |
|---|---|
| `taxon_cache_get(con, cache_key)` | Return parsed JSON (a nested list) for a cache key, or NULL on miss. |
| `taxon_cache_key_id(id)` | Cache key for a taxon looked up by id |
| `taxon_cache_key_name(name)` | Cache key for a taxon looked up by name |
| `taxon_cache_put(con, cache_key, taxon_id, value)` | Upsert a raw taxon/search response (an R list) under a cache key. |


## `scripts/inat_observations/engine/pipelines/`

### ingest_inat.R

| Function | What it does |
|---|---|
| `ingest_observations(con, place_id, taxon_id, without_taxon_id, incremental, state_path, extra_query, per_page, commit_every, throttle, request_text_fn, sleep_fn, verbose)` | Fetch observations from iNaturalist into the cache |

### ingest_plants.R

| Function | What it does |
|---|---|
| `ingest_plants(place_id, taxon_id, incremental, do_ingest, verbose)` | Fetch Point Loma plant observations into the cache |

### read_inat.R

| Function | What it does |
|---|---|
| `clear_export_cache()` | Free the memoized export frame between pipeline stages. run_pipeline calls this |
| `read_observations_export(con, resolve_taxa, request_fn, verbose, use_cache, cache_path)` | Read the cached observations as a flat table |


## `scripts/inat_observations/review/`

### qc_review_inat_location_maps.R

| Function | What it does |
|---|---|
| `build_location_review_maps(write)` | Build the review maps for records whose location looks wrong |

### qc_review_inat_misid.R

| Function | What it does |
|---|---|
| `inat_misid_qc(inat_path, specimen_path, holway_path, out_path, write, verbose)` | List iNaturalist records whose identification looks doubtful |


## `scripts/project_info/`

### collect_plant_names.R

| Function | What it does |
|---|---|
| `review_plant_names(cw_path, specimen_clean_path, plant_clean_path, prompt_fn, write, interactive_ok, worklist_path)` | Walk a human through the plant names the pipeline could not resolve |

### finding_project_info.R

| Function | What it does |
|---|---|
| `finding_project_info(write)` | Rebuild the per-survey information table |


## `scripts/project_info/review/`

### qc_review_mastercrosswalk.R

| Function | What it does |
|---|---|
| `cw_append(cw, i, col, val)` | append one value to row i's column `col`, deduped |
| `cw_get_or_add(cw, name, what_for)` | find-or-create a concept row by name; returns list(cw, i) |
| `review_unknowns(kind, cw_path, prompt_fn, write, max_items)` | Walk a human through unrecognised tags or observation fields |

### qc_review_survey_windows.R

| Function | What it does |
|---|---|
| `review_transect_ties(path, prompt_fn, write, max_items)` | Walk a human through records that matched two transects equally well |
| `review_windows(path, prompt_fn, write, max_items)` | Walk a human through records that fell outside any survey window |


## `scripts/project_info/rosters/`

### build_identification_counts.R

| Function | What it does |
|---|---|
| `build_identification_counts(out)` | Count identifications per person for the acknowledgements page |

### build_participation.R

| Function | What it does |
|---|---|
| `build_participation(out)` | Build the participation summary the acknowledgements page reads |

### roster_view.R

| Function | What it does |
|---|---|
| `roster_view(people, intern_log, years)` | Assemble the per-year view of who surveyed |


## `scripts/project_info/surveys/`

### finding_beeple_calendar.R

| Function | What it does |
|---|---|
| `finding_beeple_calendar(dir, out, write)` | Read the survey windows out of the yearly calendar PDFs |

### finding_specimen_dates.R

| Function | What it does |
|---|---|
| `finding_specimen_dates(dir, pattern)` | Returns tibble(date, n_specimens, collectors); an empty tibble if no usable .xlsx. |

### finding_survey_dates.R

| Function | What it does |
|---|---|
| `fpi_norm_transect(x)` | Normalise a transect code to one of the four the project uses |
| `fpi_survey_dates(membership, windows, roster, existing_path, review_path, intern_log_path, tol_days, people)` | Work out which survey each record belongs to |

### rescue_on_transect_surveys.R

| Function | What it does |
|---|---|
| `fpi_rescue_on_transect(membership, coords, transects_sf, off_m)` | fpi_rescue_on_transect(): given membership + per-obs coords + the transect lines, upgrade each |

### resolve_beeple_transects_per_survey.R

| Function | What it does |
|---|---|
| `resolve_transects(membership, ties_path)` | membership -> list(membership, mistags, ties). `ties` is the tie-review table (the |


## `scripts/reference/prompts/`

### manual_overrides.R

| Function | What it does |
|---|---|
| `apply_manual_overrides(df, overrides)` | apply_manual_overrides(): PURE. For each df row whose (rank, name-at-rank) matches an override, |
| `prompt_missing_taxon_ids(cache_path, overrides_path, interactive_ok, prompt_fn)` | prompt_missing_taxon_ids(): INTERACTIVE. Walk the open not_found taxa and ASK the user for each |
| `write_review_worklist(cache_path, overrides, path)` | write_review_worklist(): the file version of the prompt -- writes the open not_found set to |

### specimen_id_prompt.R

| Function | What it does |
|---|---|
| `resolve_specimen_taxa(record_df, additions_path, flags_path, fetch_fn, prompt_fn, interactive_ok, write, verbose)` | resolve_specimen_taxa(): the DRIVER. Reads the curated additions + the taxonomy flags, folds |

### verify_prompt.R

| Function | What it does |
|---|---|
| `prompt_verify_taxa(lookup_df, verified_path, rejected_path, prompt_fn, interactive_ok, write, verbose)` | prompt_verify_taxa(): DRIVER. Prompt over ALL unverified rows (previously-rejected included, so |


## `scripts/reference/refresh/`

### enrich_lookups.R

| Function | What it does |
|---|---|
| `enrich_iucn_columns(df, species_col, rank_col)` | Add iucn_code / iucn_category / assessment_year columns to a cleaned bee table. |
| `enrich_plant_common_column(df, genus_col)` | Add a plant_genus_common column (genus-level common name) to a cleaned plant table. |
| `resolve_iucn(species, force, verbose)` | Resolve IUCN status for a vector of "Genus species" names. Returns the FULL cache-backed |
| `resolve_plant_common(genera, force, verbose)` | Resolve a common name for each plant genus. Returns a named vector (genus -> common name, |


## `scripts/reference/taxonomy/`

### holway_reference_build.R

| Function | What it does |
|---|---|
| `build_holway_reference(con, holway_df, request_fn, interactive_ok)` | Resolve every Holway checklist name to an iNaturalist taxon |

### holway.R

| Function | What it does |
|---|---|
| `backfill_taxonomy(bees, genus_lookup)` | Fill blank family/subfamily/tribe from the Holway genus table |
| `clean_holway_species(species_raw)` | Strip tentative and unpublished qualifiers from a Holway epithet |
| `holway_genus_taxonomy(holway_df, warn)` | Genus-to-ancestry table built from the Holway checklist |
| `holway_qualifier(species_raw)` | The qualifier Holway put in front of an epithet, if any |
| `load_holway(path)` | Read the Holway San Diego County bee checklist |
| `split_holway_species(species_raw)` | Split a Holway epithet into species and subspecies |

### plant_lookup_join.R

| Function | What it does |
|---|---|
| `attach_flower_ids(df, lookup_path)` | Attach plant taxon ids to records carrying a flower name |
| `plant_name_parts(x)` | plant_name_parts(x): split a plant name into genus + FULL-BINOMIAL species. Vectorized. |

### plant_taxonomy_lookup_build.R

| Function | What it does |
|---|---|
| `build_plant_taxonomy_lookup(all_taxa_path, crosswalk_path, cache_path, confirmed_path, forage_path, forage_fn, resolve_fn, write, verbose)` | Build the plant taxonomy lookup -- the authority on what a plant taxon is |

### resolve_missing_ids.R

| Function | What it does |
|---|---|
| `resolve_missing_taxon_ids(df, cache_path, fetch_fn, verbose)` | resolve_missing_taxon_ids(): fill df$taxon_id for rows missing it, by iNat name search. Ranks |

### subspecies_from_cache.R

| Function | What it does |
|---|---|
| `subspecies_from_cache(lookup, fetch_fn)` | fetch_fn(taxon_id) -> the cached taxon record as a list (or NULL when not cached). |

### taxonomy_lookup_build.R

| Function | What it does |
|---|---|
| `build_taxonomy_lookup(con)` | Build the bee taxonomy lookup from the cache and the reference tables |

### taxonomy_reference.R

| Function | What it does |
|---|---|
| `ancestry_ids_from_reference(ref)` | Pull the per-rank ancestor ids out of a resolved reference table |
| `apply_verified_ids(lookup, verified_ids)` | apply_verified_ids(): PURE. Flip `verified` TRUE on any lookup row whose taxon_id is in the |
| `backfill_parent_taxonomy(lk)` | Fill blank ancestor ranks from other rows that share a lower rank |
| `build_bee_taxonomy_lookup(holway_resolved, checklist_sd_county, bees, verified_ids, ancestry_ids)` | Assemble the bee taxonomy lookup from all of its sources |
| `drop_phantom_additions(additions, spec_clean, inat_clean)` | drop_phantom_additions(): PURE. Keep a curated specimen-addition only if its taxon STILL has >= 1 |
| `load_specimen_additions(path)` | load_specimen_additions(): read the curated specimen-only additions CSV (lookup-shaped rows: |
| `specimen_additions_to_lookup(lookup, additions)` | Add taxa found only in the specimen records to the lookup |

### verify.R

| Function | What it does |
|---|---|
| `flag_new_taxa(df, sets, verified_ids)` | Add new_at_rank (comma-joined ranks new to Holway) and needs_verification |
| `load_verified_taxa(path)` | Read the user-maintained verified list; return integer taxon_ids (empty if |


## `scripts/specimens/`

### specimen_bee_clean.R

| Function | What it does |
|---|---|
| `clean_specimens(interactive_ok, prompt_fn, verbose)` | Clean the specimen spreadsheet into the analysis table |

### specimen_clean_helpers.R

| Function | What it does |
|---|---|
| `add_qc_flags(df)` | QC flags: which required-data fields are missing. genus is the one rank expected |
| `attach_lookup_taxonomy(df, lookup)` | Attach full taxonomy to specimen rows from the lookup |
| `build_complex_lookup(checklist)` | Build the species-level complex match lookup from the SD County iNat checklist |
| `build_known_names(tax_check, inat_species)` | Build the "known names" sets used by the spell-check + new-taxon detection, from the |
| `compute_taxonomy_flags(df, known_genera, known_genus_species, known_subgenera, known_complexes, known_subspecies)` | Spell-check + NEW-taxon detection. Flags, per specimen, the FINEST-rank determination |
| `detect_duplicate_ids(df)` | Duplicate ID detection: any repeated ucsd_id (should be unique), or repeated |
| `drop_non_native_apis(df)` | drop_non_native_apis(): PURE. Remove the non-native honey bee (Apis mellifera + any |
| `flag_raw_clutter(df)` | flag_raw_clutter(): PURE. The RAW-record hygiene worklist -- rows that clutter the |
| `keep_bee_specimens(df)` | keep_bee_specimens(): PURE. Keep only rows whose `family` is one of the seven bee |
| `match_plot_transect(plot, variant_map)` | match_plot_transect(): PURE. For each plot string, the transect whose (longest) |
| `match_specimen_complex(df, complex_lookup)` | Apply the species-level complex match, gated on BOTH genus and species present. A hit |
| `normalize_flower_name(x, variant_map)` | normalize_flower_name(): map each raw flower label to its canonical plant name via |
| `parse_specimen_dates(df)` | Parse the specimen date column into date_clean + month/day/year, and turn |
| `plant_variant_map(crosswalk)` | plant_variant_map(): from the crosswalk's plant rows (what_for == "plant_taxon"), |
| `resolve_determiners(codes, roster)` | resolve_determiners(): PURE. Map each specimen determination CODE ("initials + surname", e.g. |
| `resolve_review_gate(items, review_dir, interactive_ok, prompt_fn, fix_hint, blocking)` | ONE consolidated review checkpoint so nothing in the review folder gets silently |
| `sbc_bee_situation(df)` | sbc_bee_situation(): PURE. The specimen's collection situation, mirroring inat_bee_clean's |
| `standardize_specimen_names(df)` | Standard scientific-name casing: Genus title-case, species/subspecies lower. |
| `strip_control_chars(df)` | Defensive: strip embedded control/null bytes from character columns so the saved |
| `transect_variant_map(crosswalk)` | transect_variant_map(): PURE. Long (transect, variant) table from the crosswalk's |

### specimen_raw_worklist.R

| Function | What it does |
|---|---|
| `tidy_raw_specimens(write)` | tidy_raw_specimens(): read the newest raw record, flag the cluttered rows |


## `scripts/utils/`

### analysis_run.R

| Function | What it does |
|---|---|
| `analysis_tally(names, results)` | analysis_tally(): the closing line of the run. Names the scripts that failed AND the |
| `run_analysis_script(nm, dir, source_fn, fatal)` | run_analysis_script(): source one script, capturing what it warned about. |

### console.R

| Function | What it does |
|---|---|
| `bx_cont()` | (aligned continuation) |
| `bx_kv(label)` | Label   detail |
| `bx_need(what, where)` | Queue an item for the end-of-run "NEEDS YOU" rollup |
| `bx_need_print()` | Print the "NEEDS YOU" rollup at the end of a run |
| `bx_need_reset()` | Empty the "NEEDS YOU" queue |
| `bx_note()` | calm FYI |
| `bx_out()` | → output file |
| `bx_phase(n, title)` | ━━ n · TITLE ━━ |
| `bx_rule()` | ══════ |

### credentials.R

| Function | What it does |
|---|---|
| `cred_get(key, file, ask, what, prompt_fn, confirm_fn, say, is_interactive)` | cred_get(): the credential, or "" if the operator declines. Never throws. |

### ingest_mode.R

| Function | What it does |
|---|---|
| `ingest_mode_apply(flags)` | Apply what the menu returned (no-op when NULL). |
| `ingest_mode_flags(read_fn, is_interactive, preset, say)` | Ask which kind of run this is; return a named character vector of flag values to |

### people_ids.R

| Function | What it does |
|---|---|
| `people_display(ids, people)` | "p002, p001" -> "Cindy Pencek, Sam O'Dell" |
| `person_id_keys(people)` | Every written form of every person -> person_id. One lookup for the whole project: |
| `person_id_mint(n, existing)` | Mint ids for new people, never reusing one that has been issued. |
| `person_ids_in(cell, people, keys)` | A "surveyors"-style cell ("Amy Geffre, Xiomara Gaeta") -> the person_ids in it. |

### people.R

| Function | What it does |
|---|---|
| `person_name(first, last)` | "First Last", with stray whitespace squished (the roster has a typo'd trailing |
| `person_name_keys(name, first, last, code)` | Lookup vector: lowercased name-as-written -> canonical person. Covers the full |

### refresh_due.R

| Function | What it does |
|---|---|
| `refresh_confirm(overdue, read_fn, is_interactive, say)` | Ask before spending a few minutes online re-checking a year-old cache. Defaults to |
| `refresh_overdue(caches, max_age_days, today)` | Report any cache past its age limit. Returns a list (empty = all current) so the |

### utils.R

| Function | What it does |
|---|---|
| `decorate_complex(df)` | decorate_complex(): prefix "(Complex) " onto species-complex names for display, |
| `decorate_complex_name(df)` | decorate_complex_name(): mirror the "(Complex) <name>" tag into scientific_name for |
| `read_latest(folder, pattern)` | Read the most recent file matching a pattern |
| `require_columns(df, required, df_name)` | Stop unless a data frame has the columns that follow it |
| `write_fresh(x, path, row.names)` | Write a CSV over anything already at the path |


## `scripts/website/`

### build_content_pages.R

| Function | What it does |
|---|---|
| `sq(x)` | Trim a value to a plain string, turning NA into empty |

### citations.R

| Function | What it does |
|---|---|
| `citation_read_version(path)` | Read a data source's version string, if it recorded one |
| `citation_save_version(path, version)` | The version is written beside the IUCN cache when the status is refreshed, so the site |
| `inat_citation(accessed)` | inat_citation(): a credit, not a formal citation (iNaturalist requires none). |
| `iucn_citation(version, accessed)` | iucn_citation(): the required form, or "" when the version is unknown. |

### setup_notice.R

| Function | What it does |
|---|---|
| `website_setup_notice(has_site, remote)` | has_site: does docs/ already hold a built index.html? |

